import { InferDefaults } from '@/typings/props.uts'

export type PropsType = {
	/** 图标颜色 */
	color?: string
	/** 图标名称 */
	name: string
	/** 图标尺寸，支持 'small', 'medium', 'large'，'36px', '4em' 等 */
	size?: string
	/** 自定义图标前缀 */
	prefix?: string
}

export const defaultProps: InferDefaults<PropsType> = {
	color: '#000000',
	size: '32px',
	prefix: '',
}
